/**
 * Payments are embedded in order responses — there is no standalone payments endpoint.
 * Access payments through useOrders / useOrder in useCartQueries.ts.
 *
 * This file is intentionally minimal. It will be expanded when order-level
 * payment actions (refund, void) are confirmed from the v1 API.
 */
export {};
